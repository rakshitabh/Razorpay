import dotenv from 'dotenv'; dotenv.config(); const apiKey = process.env.BREVO_API_KEY; const senderEmail = process.env.EMAIL_FROM || 'bhatrakshita05@gmail.com'; async function test() { console.log('Sending test email to:', senderEmail); const response = await fetch('https://api.brevo.com/v3/smtp/email', { method: 'POST', headers: { 'accept': 'application/json', 'api-key': apiKey, 'content-type': 'application/json' }, body: JSON.stringify({ sender: { name: 'AI SOC Gateway Test', email: senderEmail }, to: [{ email: senderEmail, name: 'Test User' }], subject: 'Test Email', htmlContent: '<html><body><h1>Test</h1></body></html>' }) }); const data = await response.json().catch(() => null); console.log('Status:', response.status); console.log('Response:', data); } test();
