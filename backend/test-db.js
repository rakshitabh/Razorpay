import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); async function check() { await mongoose.connect(process.env.MONGO_URI); const User = (await import('./models/User.js')).default; const users = await User.find().sort({createdAt: -1}).limit(3); console.log('Last 3 users:', users.map(u => ({ email: u.email, isVerified: u.isVerified, otp: u.otp }))); process.exit(0); } check();
